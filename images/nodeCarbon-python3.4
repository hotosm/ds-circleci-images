FROM node:carbon

RUN apt-get update && apt-get install -y python3.4 build-essential libsqlite3-dev zlib1g-dev && \
  git clone https://github.com/mapbox/tippecanoe.git && \
  cd tippecanoe && make && make install

